<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout android:background="@drawable/bg_dialog_comment"
    android:layout_marginTop="@dimen/card_corner_10"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <View
        android:id="@+id/view"
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:layout_marginTop="30dp"
        android:background="#e1e1e1"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>
    <EditText
        android:id="@+id/et_comment"
        android:hint="Write a comment..."
        android:inputType="text"
        android:layout_width="0dp"
        android:layout_marginStart="15dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/img_send_cmt"
        app:layout_constraintEnd_toStartOf="@id/img_send_cmt"
        android:layout_height="48dp" />
    <ImageView
        android:id="@+id/img_send_cmt"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:contentDescription="@null"
        android:padding="@dimen/card_corner_10"
        android:src="@drawable/ic_send_blue" />
    <android.support.v7.widget.RecyclerView
        android:id="@+id/rc_comment"
        app:layout_constraintTop_toBottomOf="@id/view"
        android:layout_width="match_parent"
        android:layout_marginStart="5dp"
        android:layout_marginEnd="5dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toTopOf="@id/et_comment"/>
</android.support.constraint.ConstraintLayout>